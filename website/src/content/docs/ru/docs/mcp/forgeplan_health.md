---
title: forgeplan_health
description: "Показать дашборд состояния проекта — пробелы, риски, слепые пятна, сироты (артефакты без связей), просроченные доказательства и рекомендуемые дальнейшие действия. LLM не требуется."
---

Показать дашборд состояния проекта — пробелы, риски, слепые пятна, сироты (артефакты без связей), просроченные доказательства и рекомендуемые дальнейшие действия. Это самый важный инструмент MCP для ИИ-агента: вызывайте его в начале сессии, чтобы понять состояние проекта перед началом любой работы.

**Категория**: Дашборды и Графы

## Когда агент вызывает инструмент

- **Начало сессии** — первый вызов после `memory_recall` для понимания текущего состояния проекта.
- **Перед началом новой работы** — проверьте, нет ли долгов (слепых пятен, сирот, просроченных артефактов), которые следует исправить в первую очередь.
- **После спринта** — убедитесь, что ничего не ухудшилось (новые слепые пятна, осиротевшие доказательства).
- **Перед релизом** — убедитесь, что состояние чистое.

Инструмент быстрый, детерминированный и не требует LLM. Всегда предпочитайте его `list` + ручной проверке.

## Входные параметры

_Входные параметры отсутствуют. Вызовите этот инструмент с пустым объектом `{}`._

## Возвращает

Структурированный отчёт о состоянии с:

- `total_artifacts` — количество по типу (PRD/RFC/ADR/Epic/Spec/Evidence/...).
- `by_status` — разбивка по статусам: черновик / активный / просроченный / замещённый / отменённый.
- `blind_spots` — активные решения (PRD/RFC/ADR/Epic) без связанных доказательств.
- `orphans` — артефакты с нулевым количеством входящих + исходящих связей.
- `stale` — артефакты с истёкшими датами `valid_until`.
- `risks` — решения с R_eff ниже порогового значения уверенности.
- `next_actions` — упорядоченный список предлагаемых исправлений (создать доказательство, связать сироту, обновить просроченный).
- `verdict` — `"healthy"` (здоровый) или `"debt"` (долг) (наличие слепых пятен / сирот / просроченных артефактов переводит в состояние долга).

```json
{
  "verdict": "debt",
  "total_artifacts": 184,
  "blind_spots": [
    { "id": "PRD-042", "kind": "prd", "reason": "active, no evidence" }
  ],
  "orphans": [
    { "id": "NOTE-017", "kind": "note" }
  ],
  "stale": [
    { "id": "ADR-003", "days_expired": 12 }
  ],
  "next_actions": [
    "Create EvidencePack for PRD-042 and link",
    "Link NOTE-017 to parent decision or deprecate",
    "Refresh ADR-003 (expired 2026-03-30)"
  ]
}
```

## Пример вызова

```json
{}
```

## Типичная последовательность

1. `memory_recall("Forgeplan")` — восстановить контекст из предыдущих сессий.
2. `forgeplan_health` — получить текущее состояние.
3. Если `verdict == "debt"` → исправить слепые пятна / сирот **в первую очередь**, перед новой работой.
4. `forgeplan_route` — выполнить роутинг следующей задачи, как только состояние станет чистым.

## Эквивалент CLI

```bash
forgeplan health
```

## См. также

- [`forgeplan_status`](/docs/mcp/forgeplan_status/) — более простая разбивка по типу × статусу.
- [`forgeplan_blindspots`](/docs/mcp/forgeplan_blindspots/) — сфокусированный список слепых пятен.
- [`forgeplan_stale`](/docs/mcp/forgeplan_stale/) — список с истёкшими `valid_until`.
- [Руководство по методологии](/docs/methodology/overview/)