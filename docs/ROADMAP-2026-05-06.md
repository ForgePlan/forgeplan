# Forgeplan Roadmap — 2026-05-06 snapshot

> Состояние **сразу после релиза v0.29.0** (5 May 2026). Снимок открытых
> работ: PROB'ы, PRD'ы, EPIC'и + предлагаемый порядок выполнения для
> следующих ~6 месяцев.
>
> Supersedes: [`ROADMAP-2026-05-04.md`](ROADMAP-2026-05-04.md)

---

## Executive summary

| Метрика | Value |
|---------|------:|
| **Open PROBs** (active + draft) | **27** (14 active + 13 draft) |
| **Open PRDs** | **44+** (34 active + 10 draft в backlog) |
| **EPICs** | **8** (6 active + 2 draft) |
| **RFCs** | **6 active** + 1 superseded |
| Live на forgeplan.dev | v0.29.0 (через CF Pages, manual deploy) |
| Brew formula | 0.29.0 |
| Tests | **1977** |
| MCP tools | 63 |
| CLI commands | 76 |

---

## Что закрыто в v0.29.0 (для контекста)

- **PROB-029** ✅ — typed `Verdict` aggregator (`Empty / Healthy /
  NeedsAttention / Unhealthy`), `Verdict::Empty` 4th variant в Round 6.
- **PROB-049 H-1+H-4+H-6** ✅ — typed errors (`StoreTransient` /
  `StoreFatal`), `MutationContext`, `# Errors` rustdoc.
  *Followup tracked в Tier 2.*
- **PROB-050 A-4..A-15+A-30** ✅ — `claude --print` dispatch refactor,
  `claude_print::invoke()`, A-30 quality-gates docs.
  *Subsequent A-31+ tracked separately.*
- **PROB-050 A-14** ✅ — CWE-426 binary substitution closed на двух
  surfaces (env-var + struct API). Real E2E: `strings target/release/
  forgeplan | grep -c FORGEPLAN_CLAUDE_BIN` = 0.
- **PROB-051** active — Wave-1 Round 5 deferred (3 architectural HIGH +
  4 MEDIUM/LOW). *Trackt for Tier 2.*
- **PROB-052..PROB-056** new — Round 6 deferred (filed during v0.29.0
  release prep). *Tier 2.*

---

## Текущий критический долг (CRITICAL — нельзя откладывать)

| ID | Что | Почему срочно | Effort |
|----|-----|---------------|--------|
| **PROB-053** | `Delegation::Command` CWE-78 surface unguarded — no `--allow-shell` flag, no signing | Pre-existing surface; PR-E Round 6 audit MED-2; CRITICAL для marketplace plugin distribution (network-loaded YAML может execute shell) | M-L (3-5 дней) |
| **PROB-052** | CWE-426 follow-up — TOCTOU + symlink-follow в `which_in_path` | Pre-existing surface; PR-E Round 6 audit MED-1; security boundary completion (`is_file()` follows symlinks, no `canonicalize`, group-writable PATH dirs hijackable) | M (2-3 дня) |
| **PROB-051** | Wave-1 Round 5 architectural deferred (3 HIGH + 4 MED/LOW) | High-volume audit closures (phase-fold unification, perf scans, module docs) — must close before next major feature work | L (5-7 дней) |

---

## Tiered priority list

### Tier 1 — v0.29.0 ✅ (released 2026-05-05, merged at 21:24 UTC)

Verdict aggregator + typed errors + claude --print refactor + CWE-426
hardening. Tag `v0.29.0` on main; cargo-dist Release workflow auto-runs;
sync-PR `chore/sync-main-to-dev-after-v0.29.0` (PR #250) opened per red
line #9.

### Tier 2 — v0.30.0 (defensive sprint: security + tech debt, ~2 weeks)

Цель: закрыть CRITICAL math bug + Round 6 security follow-ups + paper
cuts накопленных за пред-v0.30 циклы.

**Wave 1 (security + Round 5 architectural, ~10 days):**

| Item | Что | Effort |
|------|-----|--------|
| **PROB-053** `Delegation::Command` gate | `--allow-shell` flag + workspace config opt-in + user-visible stderr warning + signing scheme design | M-L (3-5d) |
| **PROB-052** TOCTOU + symlink-follow | `canonicalize` + parent-dir ownership/mode check + dispatcher path caching | M (2-3d) |
| **PROB-051** Wave-1 Round 5 architectural (3 HIGH + 4 MED/LOW) | Phase-fold unification, perf scans, module docs — pre-existing audit follow-ups | L (5-7d) |

**Wave 2 (Round 6 medium-priority, ~4 days):**

| Item | Что | Effort |
|------|-----|--------|
| **PROB-054** `produces_at` prompt-injection | Char-set validator `^[A-Za-z0-9._/-]+$` | XS (0.5d) |
| **PROB-056** leaky verdict abstraction | Rename `verdict` → `partial_verdict` + serde compat | S (1d) |
| **PROB-049 follow-up** | Wire `is_recoverable()` first MCP retry-loop (forgeplan_health LanceDB lock contention candidate) | M (2-3d) |

**Wave 3 (paper cuts batch, ~5 days):**

| Item | Что | Effort |
|------|-----|--------|
| **PROB-028** phantom rows after .md delete | LanceDB orphan cleanup | S (1-2d) |
| **PROB-027** reindex from scratch (draft → activate) | Restore `lance/` dir capability | S (1-2d) |
| **PROB-030** BM25 search regression | `auth` prefix → 0 results bug | S (1-2d) |
| **PROB-032** score display 0.0 breakdown | UI display bug | XS (0.5d) |
| **PROB-033** new evidence blocked on fresh ws | Session state machine fix | S (1d) |
| **PROB-041** dotenvy walk-up | CLI loads `.forgeplan/.env` | XS (0.5d) |
| **PROB-038** validator FP on tech names (draft) | HTML template comment scoping | XS (0.5d) |

**Deferred к Tier 3** (если v0.30 переполнен):
- PROB-018 E2E smoke 3 bugs (M)
- PROB-020 graph integrity cleanup (M)
- PROB-051 Wave-1 Round 5 architectural (3 HIGH + 4 M/L) — отдельный
  R_eff fix sub-sprint?
- PROB-055 `claude_print` god-module 1110 LOC split (cosmetic, ~4h)

### Tier 3 — v0.31.0 (Distribution + brownfield, ~3 weeks)

| Item | Что | Effort |
|------|-----|--------|
| **CF Pages auto-deploy** (no PROB# yet) | `.github/workflows/deploy-website.yml` triggered by main push, CF API token secret | S |
| **PROB-037** MCP install/brew distribution | embed/install commands + complete brew | M |
| **PROB-045** CLI parity (9 MCP-only tools) | Add CLI commands for undo, activity, dispatch, phase | M |
| **PROB-022** Brownfield code-first protocol (draft) | Discovery protocol + source tier priority | L (potentially RFC needed) |
| **PROB-039** MCP silent failure (draft) | Capabilities + transport field fix | S |
| **PROB-047** scan-import false-positive | Document classifier improvement | M |
| **PROB-021** ADI quality (context handling) | Reasoning improvement | M |
| **PROB-018** + **PROB-020** | E2E + graph cleanups (если deferred from v0.30) | M+M |
| **PROB-055** | `claude_print` god-module split (если deferred) | S (4h) |

### Tier 4 — v0.32.0+ (Strategic + EPICs, ~4 weeks)

| Item | Что | Effort |
|------|-----|--------|
| **PROB-042** Plugin orchestration architecture | **Big strategic**: Forgeplan-as-orchestrator vs Forgeplan-as-monolith. Likely requires new ADR-012 | XL |
| **EPIC-004** CLI Discoverability + Enterprise Readiness | Open umbrella for grouped help, doctor, discover quickstart | XL |
| **EPIC-005** Phase state machine (draft) | Workflow-aware methodology | L |
| **PRD-049** Grouped help (draft) | CLI navigation improvement | M |
| **PRD-050** Doctor + Estimate table default (draft) | UX polish | M |
| **PRD-051** Discover Quickstart + FPF Explain (draft) | Onboarding improvement | M |
| **PRD-052** Audit Report MD (draft) | Audit output format | M |
| **PRD-053** LLM Provider Trait (draft) | Driver abstraction | L |
| **PRD-068** forge-history-miner skill (draft) | git log → inferred ADR drafts | M |
| **PRD-069** forge-orchestrator + ingest + scaffolder (draft) | Multi-agent skill stack | XL |

### Tier 5 — Long-term (v1.0+, deferred)

| Item | Что | Notes |
|------|-----|-------|
| **PRD-025** Nx monorepo migration | Polyglot workspace | Deferred per memory — not blocking |
| **Phase 5 — Tauri Desktop** | GUI app | Backlog Epic, no PRD yet |
| **Cleanup batch** | PROB-026, 044 (draft historical findings) | Drain when capacity allows |

---

## Sprint plan (proposed)

```mermaid
gantt
    title Forgeplan Roadmap from v0.29.0 (2026-05-06)
    dateFormat  YYYY-MM-DD
    axisFormat  %m/%d

    section v0.29.0 (DONE)
    Released                            :milestone, done, v029, 2026-05-05, 0d

    section v0.30.0 (Defensive: security + tech debt)
    PROB-053 Delegation::Command gate   :crit, prob053, 2026-05-06, 4d
    PROB-052 TOCTOU which_in_path       :crit, prob052, after prob053, 3d
    PROB-051 Wave-1 Round 5 architectural :prob051, after prob052, 7d
    PROB-054 produces_at validator      :prob054, after prob051, 1d
    PROB-056 leaky verdict abstraction  :prob056, after prob054, 1d
    PROB-049 retry-loop consumer        :prob049f, after prob056, 3d
    Paper cuts (028/027/030/032/033/041/038) :papers, after prob049f, 5d
    Tag v0.30.0                         :milestone, tag030, after papers, 0d

    section v0.31.0 (Distribution + Brownfield)
    CF Pages auto-deploy GH Actions     :cfdeploy, after tag030, 2d
    PROB-037 MCP install complete       :prob037, after cfdeploy, 4d
    PROB-045 CLI parity 9 tools         :prob045, after prob037, 4d
    PROB-022 Brownfield code-first      :crit, prob022, after prob045, 7d
    PROB-039 MCP silent failure         :prob039, after prob022, 2d
    PROB-047 scan-import classifier     :prob047, after prob039, 3d
    PROB-021 ADI context quality        :prob021, after prob047, 3d
    Tag v0.31.0                         :milestone, tag031, after prob021, 0d

    section v0.32.0 (Strategic)
    ADR-012 Plugin orchestration design :crit, adr012, after tag031, 5d
    PROB-042 Plugin orchestration impl  :prob042, after adr012, 14d
    EPIC-004 CLI Discoverability        :epic004, after prob042, 7d
    PRD-049 Grouped help                :prd049, after epic004, 4d
    PRD-050 Doctor + Estimate table     :prd050, after prd049, 4d
    PRD-051 Discover Quickstart + FPF   :prd051, after prd050, 4d
    Tag v0.32.0                         :milestone, tag032, after prd051, 0d

    section v0.33.0+ (Skills + LLM stack)
    PRD-053 LLM Provider trait          :prd053, after tag032, 7d
    PRD-068 history-miner skill         :prd068, after prd053, 5d
    PRD-069 orchestrator skill stack    :prd069, after prd068, 14d
    EPIC-005 Phase state machine        :epic005, after prd069, 10d
    PRD-052 Audit report MD             :prd052, after epic005, 4d

    section v1.0.0 (Long-term)
    Phase 5 Tauri desktop research      :tauri, 2026-09-01, 30d
    Final docs polish + 1.0 prep        :v1polish, after tauri, 14d
    Tag v1.0.0                          :milestone, tag1, after v1polish, 0d
```

---

## Reading the Gantt

**Каждый PR не блокирует другой PR**, но связан темой:
- `v0.29.0` ✅ released — critical fixes baseline (R_eff trust honesty
  через PROB-029 + Verdict::Empty)
- `v0.30.0` = defensive sprint — security closures from PR-E Round 6 +
  CRITICAL R_eff math bug + paper cuts
- `v0.31.0` = distribution + brownfield (требует RFC если PROB-022
  решено decisively)
- `v0.32.0` = strategic — `PROB-042 plugin orchestration` это самый
  большой single decision проекта (architectural identity)
- `v0.33+` = skill stack + EPIC-005 phase state machine
- `v1.0.0` = Tauri desktop + final polish

**Realistic timeline** (с учётом ~2 PR'ов в неделю sustainable pace):
- v0.29.0 → ✅ released (mid-May 2026)
- v0.30.0 → ~2 weeks (end-May 2026)
- v0.31.0 → ~3-4 weeks (June 2026)
- v0.32.0 → ~4-6 weeks (July 2026 — strategic refactor)
- v0.33-0.35 → ~2-3 months (Aug-Oct 2026)
- v1.0.0 → ~Q4 2026 / Q1 2027

---

## Risk / known unknowns

| Risk | Impact | Mitigation |
|------|--------|------------|
| **PROB-031** R_eff math fix может invalidate **существующие R_eff readouts** | Все scoring backsweep — потенциально неделя дополнительной работы | Backsweep script + `forgeplan score --all` audit при fix |
| **PROB-053** Delegation::Command gate breaks existing local playbooks (audit.yaml etc.) | Migration cost — каждый local user должен add `--allow-shell` | Add to release notes prominently + CI test that existing playbooks work с `--allow-shell` flag |
| **PROB-042 plugin orchestration** требует strategic decision о identity Forgeplan | Может развернуть scope v0.32.0 на 2-3× | Начать с ADR-012 explicitly — let user weigh in |
| **CF Pages no Git Provider** (manual deploy) | Каждый release требует ручного wrangler run | `.github/workflows/deploy-website.yml` в v0.31 закрывает navсегда |
| **27 open PROBs** | Risk of analysis paralysis при выборе priority | Tier system выше — фокус на critical + planned, остальное rolling |

---

## What's NOT in roadmap (out of scope)

- **PRD-025 Nx monorepo** — deferred per memory; не blocking; revisit when polyglot needed
- **Phase 5 Tauri** — explicitly scheduled для Q4 2026
- **PROB-026 / PROB-044** — historical handoff resolution records (drain at capacity)

---

## Update cadence

Этот roadmap — snapshot 2026-05-06. **Re-generate** перед каждым sprint
planning meeting (~each release):

1. Run `forgeplan list -t problem` для актуальных counts
2. Update Gantt sections
3. Save под new dated filename (`ROADMAP-YYYY-MM-DD.md`) — не overwrite
   previous

---

## Source artifacts

Полный список открытых работ (для cross-reference):

**Active PROBs (14)**: PROB-018, 020, 021, 028, 029 (refines applied),
030, 032, 033, 037, 041, 042, 045, 047, 051

**Draft PROBs (13)**: PROB-022, 026, 027, 038, 039, 044, 049, 050, 052,
053, 054, 055, 056

**Active PRDs (34)** + **Draft PRDs (10)**: PRD-025, 049, 050, 051, 052,
053, 055, 056, 068, 069

**EPICs (6 active + 2 draft)**: EPIC-001..008

**RFCs (6 active)**: RFC-001, 003, 004, 005, 006, 007

Cross-reference в `.forgeplan/` workspace через `forgeplan get <ID>`.

---

## Recommended next action

**v0.30.0 Wave 1, Item #1 — PROB-053 (`Delegation::Command` CWE-78 gate)**

Самый высокий security priority в backlog: `Delegation::Command { command:
Vec<String> }` parses directly from YAML без allowlist / signing /
user-visible warning. Marketplace plugin distribution готовится к
expansion (PRD-067/068/069 — orchestrator skill stack), and
network-loaded playbook YAML может execute arbitrary shell. Дальше
deferring это превратит security debt в incident.

Recommended start:
```bash
forgeplan_route "Gate Delegation::Command behind --allow-shell flag for marketplace safety"
forgeplan_get PROB-053   # read filled body (Signal + Constraints + AC)
forgeplan_reason PROB-053  # ADI: 3+ hypotheses (deep depth — irreversible API decision)
git checkout dev && git pull && git checkout -b fix/prob-053-delegation-command-gate
```

**Скиппнули PROB-031** (был в roadmap 2026-05-04 как CRITICAL) — сейчас
status `deprecated`, closed before this snapshot. Если scoring логика
имеет stale issues, открыть свежий PROB вместо реакривации.
